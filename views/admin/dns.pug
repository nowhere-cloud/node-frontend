extends ../layouts/fluid

block menu
  include ../includes/common-topbar-user

block breadcrumb
  +breadcrumb-item("/admin", "Operations Hub")

block content
  .container-fluid
    h1= title
    p Manage all DNS Alias here.
  .container-fluid.hidden-md-down
    if errorMessage.length
      .alert.alert-danger !{errorMessage}
    if successMessage.length
      .alert.alert-success !{successMessage}
    .card
      .card-header.btn-toolbar
        .btn-group.mt-0.mb-0
          a.btn.btn-block.btn-primary(href="/admin/dns/report") Generate Auditing Report
          a.btn.btn-block.btn-primary(href="#", data-toggle="modal" data-target="#NewDNS") New Entry
      table.table#dns-loadhere
        tbody
          tr
            td(colspan="7")
              include ../includes/loading-bar
      .card-footer
        a.mt-0.mb-0.btn.btn-block.btn-primary(herf="#", data-toggle="modal" data-target="#NewDNS") New Entry
  include ../includes/mobile_not_supported-f
  include ../user/_partials/dns-form
  include ../user/_partials/dns-edit
block additional-libraries
  script(src='/admin/assets/fhana.js')
