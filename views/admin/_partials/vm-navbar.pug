mixin tab-item(target, name, active)
  li.nav-item
    if active === true
      a.nav-link.active(href=`#${target}`, data-toggle='tab', role='tab')= name
    else
      a.nav-link(href=`#${target}`, data-toggle='tab', role='tab')= name

ul.nav.nav-tabs.card-header-tabs
  +tab-item('vm-hypervisor', "Instances (Hypervisor)", true)
  +tab-item('vm-record', "Instances (Registered)", false)
  +tab-item('vm-tpl-hyp', "Templates (Hypervisor)", false)
  +tab-item('vm-tpl-rcrd', "Templates (Registered)", false)
  +tab-item('vm-net', "Networks", false)
  +tab-item('vm-vif', "vNICs", false)
